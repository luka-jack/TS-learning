// 4. 元组类型 Tuple ==> 数组类型的扩展
//       元组类型用来表示已知数量和已知类型的数组, 各个元素类型不一定相同,但是元素对应位置的类型需要一致

let tup1: [string, number, boolean, undefined, null]
tup1 = ['hell0', 937, true, undefined, null]

console.log(tup1, 'log');

